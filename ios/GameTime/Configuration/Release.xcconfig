#include "PublicClient.xcconfig"

// Activity sync is staging-only through GAMETIME_ENV, and AppConfiguration
// independently locks Release contest mutations at runtime.
GAMETIME_ENV = release
GAMETIME_CONTEST_MUTATIONS_ENABLED = NO
GAMETIME_PERSONAL_SETTLEMENT_MODE = test_only
