GAMETIME_ENV = staging
SUPABASE_URL =
SUPABASE_PUBLISHABLE_KEY =
GAMETIME_CONTEST_MUTATIONS_ENABLED = YES
APS_ENVIRONMENT = development

// Optional and gitignored. It may contain only SUPABASE_URL and
// SUPABASE_PUBLISHABLE_KEY.
#include? "Secrets.xcconfig"
