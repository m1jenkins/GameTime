# Weekly local roadmap implementation and acceptance

September 6, 2026. **Local implementation and the checks below are verified.
No full W1–W4 stage is accepted while required external checks remain unperformed.** New behavior is local,
opt-in, default-off and fictional/nonredeemable. It does not admit a real Health
source or establish a launch target, price, provider clearance or actual pilot.

## Scope and provenance

The owner authorized all feasible local W1–W4 implementation, superseding the
historical W1A-only prompt. The original dirty checkout at
`/Users/user/Documents/GitHub/GameTime` and original local stack are untouched.
The isolated checkout is `/Users/user/firstmate/projects/GameTime`, branch
`codex/weekly-local-roadmap`. Two preexisting unpublished commits and commit
`5654268` preserve the historical commitment/engagement prerequisites separately.
The [execution checklist](WEEKLY_EXECUTION.md) maps ownership and dependencies.

Personal, Solo, legacy charity and historical fictional-5K agreements retain
their contracts. Organizer-event nomination screens remain paused. Two to five total
friend participants including the creator remains D132's working assumption.
Community entrants individually pursue one common configured fixture target.

## Implemented versus accepted

| Slice | Local implementation | Acceptance boundary |
| --- | --- | --- |
| W1A | Separate versioned weekly-steps terms and pure qualification evaluator; exact consent, calendar/precision, incomplete-data and correction fixtures | Focused evaluator accepted as fictional pure logic only; see [W1A evidence](WEEKLY_STEPS_POLICY_V1_ACCEPTANCE.md) |
| W1B source | Unwired default-off Health probe and diagnostic source model | Device matrix unperformed; no real-source qualification or confirmed-miss admission |
| W1B / W2A backend | Separate private weekly domain, actor/session-bound RPCs, frozen consent, friend/community enrollment, display-only progress, service-only fictional observations, full review lifecycle, immutable results and conserved simulation | All 143 focused DB assertions, blocking races and clean-reset persisted lifecycle checks passed; real-source admission remains disabled |
| W1C / W2B native | Loopback-only opt-in weekly journey, explicit rules/consent, progress, review/results, exits/support, pause and exact durable request recovery | Debug/Staging/Release compilation, native unit/rendered fixtures and authenticated HTTP integration passed; closed-invitation safety verified; human accessibility separate |
| W3 | Distinct Exercise-minute source/metric fixture contracts; no workout-duration substitution | Source/manual lineage unavailable; ordinary selectable Exercise mode disabled |
| W4 | Distinct cumulative and whole-run timed-distance policies, exact units/comparators and local native fixture prototype | No mandatory 5K; 15 native persistence/recovery/account-isolation tests and the four-format native-to-TypeScript bridge passed; real-source field acceptance unavailable |
| Responsible engagement | Existing comprehension/push cleanup preserved; new consent/exit controls, revocable local study records and unwired reminder suppression rehearsal | Reminder rehearsal passes 15 tests but cannot deliver; no notification or real pilot acceptance |

## Checks actually run in this execution

Historical acceptance counts are not repeated here as fresh evidence. Counts
below identify the source state at their run; subsequent native-only corrections are covered by the final native target
and its separate authenticated HTTP run. The delivery pipeline also validates
the committed branch.

| Check | Executed evidence |
| --- | --- |
| W1A focused pure policy | 162 tests passed; [dated evidence](WEEKLY_STEPS_POLICY_V1_ACCEPTANCE.md) |
| Source/metric prototypes | 20 Deno and 10 Swift tests, Debug build and one Simulator default-off probe test covering all three metrics passed; [source evidence](WEEKLY_SOURCE_METRICS_ACCEPTANCE.md) |
| Full current Deno functions | Format, lint, type check and 833 tests passed in the clean disposable replay |
| Existing portable Swift core including source diagnostic | `swift build && swift test`: 113 tests across 10 suites passed; `/tmp/gametime-weekly-core-regression.log` |
| Weekly focused pgTAP | 143 assertions passed: lifecycle 77, blocking-race 25, sharing/expiry 41; final clean replay incorporates the five absence/zero completeness regressions |
| Persisted DB → real TypeScript evaluator → optimistic worker | Passed 2/3/4/5 participants, downward correction, stale snapshot, full new notices, review timeout, simulation conservation/retry, community all/none/some handling, delayed-worker refunds, and early worker ticks before the remaining two-/five-person consents; clean replay passed |
| Clean portable replay | `scripts/weekly-local-verify.sh` passed at `121628a`: 72 SQL files / 3,576 assertions, 833 Deno tests, 113 Swift tests / 10 suites, and the persisted lifecycle smoke; tracked-input manifest and logs under `/tmp/gametime-weekly-verify.fL5v687w`; its disposable stack stopped |
| Additional configuration / policy gates | Final Staging and Release simulator builds passed (`/tmp/gametime-weekly-final-staging-build.log`, `/tmp/gametime-weekly-final-release-build.log`); 10 conformance tests passed; beta preflight fixtures and Personal automatic-copy audit passed |
| Independent review | [Final finding ledger](WEEKLY_INDEPENDENT_REVIEW.md): all actionable findings closed within the stated local boundary using their named regression evidence |
| Native models, persistence and safety | 18 weekly tests and 15 distance-prototype tests passed, including held optional study transport, scheduled and monotonic privacy expiry, exact pending bytes, deletion fences and account draft erasure |
| Rendered weekly fixtures | Four tests / five actual SwiftUI PNGs passed crop-based OCR and lead plus independent visual inspection: all five named targets before consent, accessibility3 text, solo community rules, provisional/final/refund and recovery; expired unaccepted invitations show a closed explanation and retain support |
| Complete native test target | `GameTimeTests`: 367 tests, 3 controller-gated skips, zero failures; `/tmp/gametime-weekly-final-all-native-units.log`. Weekly HTTP ran separately below; historical duel/performance HTTP controllers were not rerun in this execution |
| Legacy product UI regression | 50 tests passed: 11 duel, 31 Personal and 8 existing performance-commitment UI tests; `/tmp/gametime-weekly-full-native.log`. That original mixed run had an earlier weekly rendering-harness failure; all four corrected weekly render tests subsequently passed. This is not a claim that the original mixed command exited successfully |
| Real native Auth / HTTP | `tmp/weekly-native-c8d692d1.xcresult`: passed the complete two-/five-person, early-worker consent, solo next-week community, full historical review→final worker, exact committed response-loss recovery, uncommitted retirement, bilateral sharing, held account-switch, pause/gate-off support and exits; cleanup verified admission off / allowlist empty / scoped sessions revoked |

## Reproduction

Use a committed checkout with Docker, Supabase CLI 2.109.1, PostgreSQL tools,
Deno, Swift and Python 3 installed:

```sh
./scripts/weekly-local-verify.sh
```

The script creates a unique disposable local Supabase project on loopback
ports `57320`–`57329`, copies only tracked test inputs, writes a SHA256 input
manifest and source commit, resets that disposable database from migrations,
runs all portable suites and the persisted weekly lifecycle smoke, then stops
only its own project. The temporary source, manifest and logs remain at the
printed path. Choose `WEEKLY_VERIFY_PORT_BASE` if those ports are occupied;
`--keep-stack` intentionally leaves only that disposable project running.
No local `.env`, linked-project state or generated build directory is copied.
This does not reset the original development database on `54322`.

Native verification additionally needs Xcode and an available iOS Simulator.
The local toolchain differs from the pinned CI Xcode 26.2 toolchain; CI results
must be reported separately. See [native reproduction and evidence](WEEKLY_NATIVE_V1_ACCEPTANCE.md).

## Missing evidence and exact next actions

| Gate | Missing evidence / next action | Capability kept disabled |
| --- | --- | --- |
| Physical steps source | Execute and record the iPhone/Watch matrix in [source acceptance](WEEKLY_SOURCE_METRICS_ACCEPTANCE.md), including manual/imported identity, duplicates, deletions/downward corrections, late/locked/offline sync and permission/source loss | Real weekly qualifying uploads and any completeness/confirmed-miss claim |
| Exercise lineage | Observe generated Exercise-time provenance for manual/imported and approved Watch workouts, overlaps and corrections; define an explicit new policy if reliable filtering is impossible | Selectable real Exercise metric |
| Distance evidence | Validate selected-distance accuracy, full elapsed pauses, route/segment semantics and correction/deletion behavior on devices; establish separate proof-of-miss handling | Real cumulative/timed-distance admission |
| Human acceptance | Run VoiceOver, Dynamic Type, comprehension and voluntary-exit checks on the actual flows with people; record failures and fixes | Full native/accessibility stage acceptance |
| Pilot | After source/native/privacy/support acceptance, obtain separate recruitment/distribution authorization, run two seven-day rounds with 20–30 adults including solo joiners and 2–5 friend groups, record descriptive fairness/comprehension/pressure/privacy/review/voluntary-return evidence | Public cohort rollout and any claim of observed pilot outcomes |
| External reminders | Complete contextual OS consent, durable category preferences, routing and server suppression/deduplication integration; obtain delivery authorization | Notifications and external messages |
| Funded version | Select approved amounts/fees/recipient/limits and delayed increases, enforce aggregate exposure across products, then obtain provider/legal/jurisdiction/platform and source/pilot clearance | All live money, provider operations and redeemable allocation |

No hosted deployment, participant recruitment, external message, notification
enablement, payment-provider operation or merge occurred in this execution.
