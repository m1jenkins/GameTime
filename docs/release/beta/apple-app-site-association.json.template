{
  "applinks": {
    "details": [{
      "appIDs": ["REQUIRES_APPROVED_APP_ID_PREFIX.REQUIRES_APPROVED_RELEASE_BUNDLE_ID"],
      "components": [
        {"/": "/challenge-invite/*", "?": "?*", "exclude": true, "comment": "Do not open invitations with a nonempty query."},
        {"/": "/challenge-invite/*", "#": "?*", "exclude": true, "comment": "Do not open invitations with a nonempty fragment."},
        {"/": "/challenge-invite/????????????????????????????????????????????????????????????????", "caseSensitive": true, "percentEncoded": true, "comment": "Opaque locator only. The app also requires lowercase hexadecimal, the exact configured HTTPS origin, and no query or fragment, including empty delimiters."}
      ]
    }]
  }
}
