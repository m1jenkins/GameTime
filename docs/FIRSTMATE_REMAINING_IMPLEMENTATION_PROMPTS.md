# GameTime remaining prompts

Reevaluated September 13, 2026. Use the [working baseline](WORKING_BASELINE.md)
and [remaining plan](GAMETIME_REMAINING_IMPLEMENTATION_PLAN.md).
P0/0A/3 and P4/P5/P6 are implemented locally; P2's measurements are historical.
P7 is prepared, with physical acceptance pending. P10's independent preparation
is complete. Keep those records; do not rerun completed prompts.

Signal is the official native UI/UX. P9A's [local implementation and verification](../outputs/reports/2026-09-13-signal-native-migration.md)
now correspond to native source published on `main` at `b351a47`; preserve this work for P9.
The earlier unlocated completion report remains unverified. The P9A prompt below
preserves its acceptance contract and is not a request to restart the migration.
P8/P9 may proceed
one accepted metric at a time. All four sources and all 13 policies remain
required before distribution.

The owner has no Apple Watch yet and selected simulator-only work. Do not start
the physical P7 prompt or request Watch actions now. The bounded P11A local
recovery slice is implemented on an unlanded task branch; its [report](../outputs/reports/2026-09-14-p11a-local-recovery.md)
records the combined source, interfaces and validation limits.
The [September 14 community follow-up](../outputs/reports/2026-09-14-p11a-community-verification.md)
closes the verification gap and recommends separate local landing approval for
its exact reviewed tip; it does not authorize another implementation slice.
This remains local implementation evidence, not real P7/P8/P9 or hosted acceptance. The next
dependency is an accepted real-source contract and a separately authorized
hosted target. The physical-source and real-integration prompts below retain
their future acceptance contracts until those dependencies are available.

## Common instructions — prepend to the selected task

```text
Work in /Users/user/Documents/GitHub/GameTime from current main. Read AGENTS.md,
PROJECT_MEMORY.md, docs/WORKING_BASELINE.md, docs/BETA_IMPLEMENTATION_PLAN.md and
docs/GAMETIME_REMAINING_IMPLEMENTATION_PLAN.md. Read docs/COPY.md before changing
app strings. Implement only the task selected below; drafting this pack does not
execute it or authorize later tasks.

Inspect status, existing branches and the selected native candidate. Reuse completed
work. Use a short-lived branch; a worktree only if isolation is needed. Preserve
unrelated changes, archived work and other tasks' databases/devices. Do not clone
the project again, import the old dirty patch, or restart cancelled gate recovery.

Preserve historical agreements, consent, admission limits, exact retries, privacy,
revocation, correction/review windows and safe exits. Use forward migrations.
Keep unaccepted sources disabled and all money nonredeemable simulation. Never
use fixture actors, clocks or client-authored finality to enable real operation.

Use focused checks and one concise review on final source. SQL changes need an
owned disposable DB, affected tests, relevant actual-session races and an old-data
upgrade/historical smoke. Native changes need the affected build/journey. Reserve
the full release matrix and long soak for P11/P12. Do not build another testing or
infrastructure framework. Use the plan's bounded validation defaults; record a
repeated environment obstacle and continue independent work without calling the
untested invariant accepted.

End with source identity, changed behavior, actual checks, limitations and the
next dependency. Distinguish local implementation from physical/hosted/human
acceptance. Stop after the selected task. Push, merge, hosted mutations, device
Health access, messages to people, distribution, recruitment and destructive
cleanup require their existing explicit authorization; this pack grants none.
```

## Prompt 7 — finish physical source decisions

```text
Resume the existing P7 checkpoint and docs/BETA_PHYSICAL_SESSIONS.md after explicit
opt-in naming the iPhone and paired Watch. Reuse the prepared investigation;
rebuild only when the source or available resources require it. The person controls
Health actions; keep raw records and accuracy measurements on-device and retain
only the guide's prescribed categorical findings.

Investigate steps first and Exercise/cumulative/timed running early. Complete
source/overlap, manual/imported data, edits/deletion, delayed sync, permissions,
lock/offline, workout/pause and frozen time-boundary cases. Separate observations
from approved policy. For each metric return the exact eligibility/reconciliation,
freshness, correction and missing-data rules; timed running needs a measured,
approved whole-workout distance band.

Accept a source only when its result rules are defensible. Empty reads cannot
establish denial, readiness, zero or a miss. If a source cannot support the current
contract, report the specific limitation and decision needed; do not extend the
investigation indefinitely or quietly reduce the all-mode release requirement.
Hand accepted metric policies to P8/P9 while other physical cases remain open.
Do not start ingestion, scoring or hosting in this task.
```

## Prompt 8 — implement accepted real facts and server processing

```text
Implement the real-source backend for the accepted P7 policy or policies, starting
with steps when available. Reuse existing challenge policy/evaluation, session,
request and worker primitives. Preserve fictional agreements under their original
versions. Do not invent rules for unaccepted metrics.

Add versioned real agreement/readiness/ingestion contracts and a separate real
admission, processing and community-publication boundary. Current runtime and
workers require fixtures; changing a flag or dropping a CHECK is not sufficient.
Keep new controls default-off with recoverable reads, reviews, exits and exact
committed retries while admission is paused.

Accept only actor/challenge/terms/source-policy binding, frozen window, normalized
replacement value or deleted/unresolved state, monotonic revision, freshness,
exact request identity and required integrity binding. Reuse attestation mechanisms
where compatible. App authenticity does not prove activity completeness. Derive
results on the server using accepted rules; reject raw samples/routes/source names,
baselines, arbitrary policies and client-authored complete/verified/final flags.

Test wrong actor/terms, stale sessions, replay, response loss, concurrent revisions,
downward correction/deletion, late facts and full notice/review windows. Prove the
new facts reach provisional and final simulated results without fixture shortcuts.

Include bounded reproduction of the existing overnight report's Privacy1 reviewer
read/revocation race and S1 create/suspension concern. Privacy1 is source-confirmed
but runtime-unverified; S1 is an unverified hypothesis. Fix reproduced defects with
focused actual-session proof; retain any untested finding honestly. Do not restart
P4 or a general backend audit. Run affected SQL/RLS/ingestion and upgrade checks.
```

## Prompt 9A — make Signal the default and retire cobalt (local implementation recorded)

```text
Implement docs/design/SIGNAL_UI_MIGRATION.md. Signal is the official design;
cobalt must be fully retired from active presentation. Inspect available native
work once and reuse verified applicable changes. If no Signal candidate exists,
implement the missing native presentation from the adopted September 13 Signal
study and its refinements. Do not start another visual exploration.

Migrate semantic tokens, typography, components, controls, charts and every
reachable route, including launch/sign-in/onboarding, all new-product journeys,
account/support/privacy and retained Personal access. Make Signal the ordinary
default with no opt-in flag or selectable cobalt fallback. Remove superseded
cobalt rendering and unused fonts/assets/build inputs after checking consumers.
Preserve historical evidence, agreements and consent; legacy product retirement
has separate acceptance. A palette change or class rename does not finish this.

Use current stores and explicit local fixtures with closed external clients;
P7 physical acceptance and hosted configuration do not block presentation work.
Keep data opaque, native control/material fallbacks accessible and missing values
unknown. Do not widen Health uploads or friend visibility for charts. Read
COPY.md before app strings. Preserve product rules, retries, privacy and exits.

Build and run affected rendering/journey checks on final source. Record route
coverage, normal signed-out/signed-in screenshots, all 13 policies, two/six-person
layouts, compact/large text, light/dark and solid/reduced-motion states. Report
unperformed physical/human checks separately. Hand Signal to P9 for real-contract
integration; do not claim transport, source or release acceptance from UI work.
```

## Prompt 9 — connect the native product end to end

```text
Connect the official Signal UI from P9A to accepted P8 contracts. Verify its
migration acceptance and tested source; finish any missing required presentation
under docs/design/SIGNAL_UI_MIGRATION.md. Review
and carry forward the applicable existing native fixes on
codex/overnight-integration-20260913 (code a3e7733, report 264bbd0) through the
established delivery path. Do not implement the same fixes twice or blindly merge
the entire branch. Preserve the Signal presentation while integrating applicable fixes.

Complete one accepted steps journey through activity, correction, review and final
history before expanding the same path to the other three accepted metrics. Reuse
ChallengeHealth contracts/read-session/revision machinery and ChallengeV1Store.
Use canonical units: steps; Exercise integer seconds; running integer millimetres;
whole timed runs with end-start elapsed seconds and the accepted distance band.
Keep seven readiness states, on-device suggestions, account/terms-bound protected
cache, stale-response fences, exact retries and lower/deleted-value recovery.
Complete background callbacks promptly; observe real lock/offline/late-sync behavior
only within authorized device sessions.

Browsing/drafting must work without Health. Final consent needs eligible history
within 30 days for cumulative metrics or a comparable whole workout within 90 days
for timed running. A positive read establishes availability, not completeness.
Suggestions remain on-device and absent for leaderboards or insufficient history.

Replace the ordinary unavailable challenge client with the reviewed authenticated
HTTPS client configuration for the approved target. Reuse native Apple sign-in,
21+ confirmation and exact username lookup. Preserve invitation intent through
cold/warm launch, sign-in cancellation/failure and account change. Implement HTTPS
link parsing/routing and the matching associated-domain configuration; a local
custom scheme or AASA template alone is insufficient. Do not select an old hosted
project by default. Keep incomplete external configuration closed and report it.

Verify friend invitation, own targets, roster freeze/reconsent, start, progress,
correction, review, result and safe exit; personal creation/results; community
preview/join/leave, delayed counts, reports and appeals. Cover all 13 policies and
two/six-person journeys using existing tests. Preserve historical Personal access
until replacement acceptance. Distinguish fictional HTTP tests from device/hosted
proof. Render only authorized real data; omit unavailable chart detail rather than
inventing points or widening Health uploads. Reuse working screens and defer optional
visual polish. Required Signal migration and cobalt retirement are P9A acceptance
work, not optional polish; do not leave cobalt as the default or a fallback.
```

## Prompt 10 — completed preparation; resolve its inputs

Do not run another P10 preparation task. Reuse the
[completion report](../outputs/reports/2026-09-12-p10-completion.md),
[hosted plan](BETA_HOSTED_PREPARATION.md), [support/retention plan](BETA_SUPPORT_RETENTION_PREPARATION.md)
and [existing worksheet](release/beta/hosted-settings.draft.json).
Resolve three groups of owner inputs as the dependent work becomes concrete:

- **Environment:** separate project/region/budget, Apple/bundle/HTTPS-link identity,
  credential custody, candidate and exact permitted hosted actions.
- **Operation:** entity/regions, privacy/terms/support URLs, staffed support and
  independent reviewers, retention/deletion/backup rules.
- **Community:** source-informed target, outcome minimum, publication capacity,
  dates/timezone and simulated amount. Planning capacity is not publication approval.

## Prompt 11A — finish the operating code locally

The captain's completed local slice is limited to worker/scheduler recovery:
durable invocation identity, scoped dispatch, bounded retries, individual
dead-letter recovery, delayed community snapshots and sanitized local status.
Operator-tooling expansion, account deletion and retention-policy-dependent work
remain deferred and are not implied by the report below.

```text
Turn P10's prepared operation into the minimum working local implementation, using
its approved decisions where required. Reuse the claim/complete worker: add durable
trigger/request identity, scoped invocation, bounded failure recovery and audited
item-specific dead-letter recovery. Add the delayed community snapshot job and
sanitized heartbeat/backlog/failure alerts. Keep real credentials and schedules off.

Provide the smallest usable authenticated operator tooling for review, reports,
removal, global support and independent appeals, with grant expiry/revocation and
least privilege. An audited CLI is sufficient; do not build an admin platform.

Implement in-app deletion initiation and the approved versioned new-domain
retention/deletion workflow, including session/Apple-token revocation, minimum
retained audit/holds, device-cache clearing and idempotent completion. Preserve
historical agreements and test restored-data revocation/deletion handling. If policy
is still unapproved, complete independent operating code and identify this specific
remaining dependency; do not invent retention periods or call a runbook completion.

Prepare the exact hosted application plan, including RPC/Edge allowlists and
prevention of historical cron execution during migration application. Exercise
pause, response loss, expired claims, deadline preservation, privacy and recovery
on an owned disposable local stack. No provisioning, hosted credential changes,
external messages or deletion of user data in this local task.
```

## Prompt 11B — deploy and verify the authorized Beta backend

```text
After approval of the exact target, candidate, settings, cost and actions, apply
the reviewed P11A deployment plan. Keep unaccepted capabilities closed. Confirm
actual role/session denials, Apple identity and HTTPS invitations, scoped operator
access, real worker/snapshot schedules, sanitized monitoring, pause and recovery.
Verify the approved support route only with authorization for its test message.

Adapt the existing load harness to the real contracts on an authorized isolated
environment, using synthetic traffic rather than participants' data or real Health
records. Run the remaining plan's 2,000-account/250-DAU/100-concurrent/25-rps and
250-person community workload, outage/replay drills and one two-hour soak. Preserve
P2's old failures. Measure against the existing budgets and fix actual bottlenecks;
do not add 25k/10k-community characterization or speculative infrastructure.

Return exact deployed identity, observed results, recovery proof and any remaining
blockers. Hosting success does not authorize TestFlight, recruitment or money.
```

## Prompt 12 — qualify the integrated release candidate

```text
Review the integrated native/source/hosted candidate once and fix concrete defects.
Verify all 13 policies, two/six-person journeys, consent/review/exits, community
privacy, expired sessions, revocation/deletion, exact recovery and support routes.
Run the full relevant SQL/native/historical/controller matrix, minimum/current iOS,
supported sanitizers and Release packaging. Reuse existing controllers and record
runtime gaps/skips honestly. Repeat only affected checks after fixes, then verify
the actual final signed binary and its source identity.

Check ordinary Apple sign-in and approved HTTPS transport; no enabled fixtures,
fictional clock, payment path or Watch app in the distributed product. Preserve
legacy Personal access until the replacement's required local/physical acceptance;
retire its creation/navigation/client/background wiring only after that acceptance,
then rerun affected regressions and packaging. No historical bulk deletion.

Return a concise candidate decision, the actual evidence and the remaining P13
human/device checks. A green software suite is not physical or release acceptance.
```

## Prompt 13 — human acceptance, private Beta and launch handoff

```text
With the existing exact device opt-in, finish physical all-source recovery and
accessibility on the final candidate. Check VoiceOver, large text, Reduce Motion,
Reduce Transparency, contrast, offline/locked recovery and actual two/six-person
consent/result/exit comprehension. Reuse still-applicable P7 evidence; do not repeat
private sessions without a changed behavior or unresolved case. Confirm operating
support, approved privacy/terms/deletion and community settings.

Resolve privacy, wrong-result, consent, unsafe-exit and other release-blocking
findings. Record only gates supported by their actual acceptance and authorization;
real money and optional analytics remain off. Coordinate final packaging after any
accepted replacement changes. Prepare TestFlight metadata, feedback contact and
review access with accurate Watch/Health/simulation instructions. Distribution and
recruitment happen only after their exact authorization and applicable Apple review.

Once separately authorized, run the agreed small simulated pilot through complete
activity/correction/review/result cycles. Predeclare the cohort and checks; the older
20–30-person/two-round proposal is not evidence or a mandatory headcount. Use current
2–6-person rules and all four sources. Manual feedback is enough; record missing
updates, misunderstandings, result corrections, support burden, voluntary return and
pressure/exit problems. Do not build analytics or growth features to run the pilot.

Recommend public simulated launch only after concrete Beta issues are resolved.
Prepare the App Store build/metadata/screenshots/privacy declarations, supported
regions, review access and operating/release plan; verify current Apple requirements.
Submission/public release need explicit authorization. Funded launch stays in the
separate PLAN.md Phase 6 workstream. Report actual readiness, not a promise of Apple
approval or a launch date unsupported by physical/Beta results.
```
