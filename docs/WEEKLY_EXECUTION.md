# Weekly implementation execution record

September 6, 2026. The owner authorized all feasible local W1–W4 work,
superseding the older W1A-only prompt. That local implementation is complete
within the boundaries below. The current follow-up authorizes reconciling and
merging PR 17 after its checks pass, then a separate physical steps-source Debug
investigation branch. It does not waive acceptance or external gates.

## Working tree and delivery

At initial intake, `/Users/user/Documents/GitHub/GameTime` was dirty on `main`
at `9139679`; the local implementation left it untouched. Work used an isolated
clone at `/Users/user/firstmate/projects/GameTime`, branch
`codex/weekly-local-roadmap`. At that intake GitHub `main` was at `c403b88`, and
the original checkout had two earlier unpublished commits (`e9ed7cc`, `9139679`),
retained in the branch. These are historical intake facts, not current branch
positions.
Commit `5654268` preserves the existing commitment/engagement implementation and
current roadmap prerequisites separately from new weekly implementation.
Installed skills, generated outputs and `.ua` artifacts are excluded.
GitHub intake found no open PRs and only the unrelated historical charity
catalog issue. [PR 17](https://github.com/m1jenkins/GameTime/pull/17) now carries
the implementation and acceptance evidence. The owner authorized marking it
ready after status reconciliation and passing checks, then merging while
preserving useful commits. The original checkout's uncommitted changes must be
compared and preserved; differing or unrelated work must not be discarded.

For final reconciliation, upstream `main` had advanced to `dcefd79`, and the
existing monitor rebased the branch to `db9d3127` after the GitHub conflict.
Root's independent range-diff marked all 20 retained patches equal, and all 57
paths from omitted prerequisite `5654268` were byte-identical in upstream
`dcefd79`. Read-only inspection found the original checkout on `main` at
`dcefd79` with only the two-line `DeleteAccountView.State` to
`DeleteAccountState` rename; its added and removed lines match old PR tip
`6476d474`, while the complete file differs because the PR contains other
changes. The original checkout remains untouched.

## Coordinated checklist

| Requirement | Dependencies / owner and files | Acceptance and current evidence |
| --- | --- | --- |
| W1A exact fictional weekly policy | Policy owner: new `_shared/weekly-steps.ts` and tests | Implemented and locally accepted as fictional policy logic; 162 focused tests and full 775-test Deno run passed at policy completion. All decisions remain provisional. |
| W1B actual step source | Source owner: `WeeklySourceFeasibility.swift`, `WeeklyHealthSourceProbe.swift` | Default-off unwired prototype; physical source acceptance is unperformed. The next authorized branch adds a visibly identified Debug investigation for an explicitly opted-in iPhone/Watch. A successful or empty query never proves complete capture. |
| W1B local fictional agreements/lifecycle | Backend owner: new `weekly_local_v1` migration, weekly pgTAP/worker files | Implemented and verified: active sessions, exact consent/retries, 2–5 roster, correction/full review, immutable results, conservative exits, simulation conservation, overlap/pending limits. Full clean SQL and persisted-worker replay passed. |
| W1C native friend journey | Native owner: new `Weekly*` models/client/store/views/tests and narrowly owned app wiring | Implemented and locally verified; two-/five-actor native HTTP, recovery/privacy, rendered fixtures and Debug/Staging/Release configuration checks passed. Closed-invitation display regression also passed; human accessibility remains separate. |
| W2A community enrollment/simulation | Same backend/migration owner, separate community policy in new domain | Implemented; common fixture target, individual completion, server cohort, minimum/capacity, private own/counts projection, immutable allocation and cross-mode overlap pass the clean SQL/worker replay. |
| W2B native community and measurement | Native owner plus integration owner | Implemented and locally verified; real local Auth/native HTTP passed solo join and a deliberately selected fresh second week, preview/review/exits. Descriptive opt-in local instrumentation only; recruitment/distribution and actual pilot remain unperformed. |
| W3 Exercise minutes | Source/metric owner: new `_shared/weekly-metric-fixtures.ts` | Fixture/prototype contracts only; separate fixture policy; no substitution of workout duration or Garmin intensity. Manual/imported lineage device evidence blocks real-source selection. |
| W4 cumulative/timed selected distance | Same source/metric owner | Fixture/prototype contracts only: separate fixture contracts and private native notebook; 15 native tests and four-format evaluator bridge passed. Exact units and full elapsed semantics, no mandatory 5K. Device proof and proof-of-miss handling block actual source admission. |
| Responsible engagement | Integration owner; backend owns all schema changes | Consent, neutral exits, default-off push, no loss targeting, no automatic next week. Pure optional reminder rehearsal passes 15 tests; it cannot send. Local measurement consent/revocation and coarse events are under backend/native verification. |
| Independent acceptance review | Review owner after implementation; lead resolves findings | Independent security/privacy, concurrency, consent/recovery and regression review completed; actionable findings corrected with named executed regressions. The separate source/metric work was reviewed by the lead. |
| Reproducibility | Lead | Disposable native stack `gametime-weekly-local`, API 56321 / DB 56322. Original stack 54321/54322 untouched. Independent tracked-input fresh stack passed 3,576 SQL assertions, 833 Deno tests, 113 Swift core tests and persisted lifecycle. Authenticated HTTP and weekly native/model/rendered checks passed; complete native target enumerated 367 tests (364 passed, 3 gated controller skips), all 50 legacy UI tests passed, and final configuration builds passed. The separate PR 17 CI snapshot is recorded below. |

## Delivery CI and next acceptance work

The PR 17 CI snapshot at `6476d47` passed 3,576 SQL assertions, 835 Deno tests,
113 Swift core tests, 365 native tests with three controller-gated skips,
50 legacy UI tests, 10 conformance tests, and Staging/Release builds. This is
separate evidence from the earlier local 833-Deno and 367-enumerated-native run.
The required checks for the final reconciled PR head must pass before merge.

After merge, the focused next branch is physical weekly steps-source acceptance,
not more fictional backend breadth. Follow the existing source matrix, keep the
investigation Debug-only, explicit opt-in and private on-device, and disconnect it
from scoring, uploads, telemetry and logs. Stop for required human actions and
record only observations actually performed. Completeness and trustworthy miss
handling remain unresolved, so real weekly scoring stays disabled.

Human VoiceOver, Dynamic Type, comprehension and voluntary-exit tests remain next
after or alongside source investigation. Physical source validation, those human
checks, a real pilot, hosted operation, notifications and funded operation are
all unaccepted. The proposed 20–30-person pilot needs separate authorization.

## Unchanged gates

- Five means 2–5 total including creator only as the documented working
  interpretation. Numeric targets and example cents are fixtures, not approved
  health recommendations, launch targets or prices.
- Personal, Solo, charity and fictional official-5K contracts retain their
  original versioned behavior. Organizer-event screens remain paused.
- Required device records must cover identity/manual/imported data, overlaps,
  reimports, deletions/downward revisions, late sync, locked/offline behavior,
  permission/source loss, timezone changes and metric precision.
- Human VoiceOver/Dynamic Type/comprehension evidence and a separately
  authorized two-round pilot cannot be replaced by fixtures or simulator tests.
- No hosted mutation, deployment, recruitment, external messages, notification
  enablement, payment-provider operation or live money is authorized.
- Funded admission needs selected numerical limits and delayed increases,
  complete cross-product enforcement, provider/legal/platform clearance and
  source/pilot evidence. New weekly fixture limits do not establish that gate.
