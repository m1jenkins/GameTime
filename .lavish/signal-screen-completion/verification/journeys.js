const results=[];
const check=async(name,fn)=>{try{await fn();results.push({name,pass:true});}catch(e){results.push({name,pass:false,error:e.message});}};
const assert=(x,m)=>{if(!x)throw Error(m)};
const click=sel=>page.eval(`() => {const el=document.querySelector(${JSON.stringify(sel)});if(!el)throw Error('Missing: '+${JSON.stringify(sel)});if(el.disabled)throw Error('Disabled: '+${JSON.stringify(sel)});el.click();}`);
const go=route=>page.eval(`() => SignalDemo.go('${route}',true)`);
const val=(sel,v)=>page.eval(`() => {const e=document.querySelector(${JSON.stringify(sel)});if(!e)throw Error('No field');e.value=${JSON.stringify(v)};e.dispatchEvent(new Event('input',{bubbles:true}));e.dispatchEvent(new Event('change',{bubbles:true}));}`);
const get=()=>page.eval(()=>SignalDemo.state);
await check('Home activity / chart / Back retention',async()=>{await page.eval(()=>SignalDemo.reset());await click('[data-action="metric:minutes"]');await val('#day-selector','6');assert(await page.eval(()=>document.querySelector('.chart-readout').textContent.includes('Not available')),'unknown readout');await go('challengeDetail');await click('[data-action="back"]');let s=await get();assert(s.metric==='minutes'&&s.day===6,'selection preserved');});
await check('Invitation request → roster → proposal → explicit consent → confirmation',async()=>{await page.eval(()=>SignalDemo.reset());await go('invitation');await click('[data-action="request"]');assert((await get()).route==='lobby','lobby');await click('[data-action="demo-select"]');await click('[data-check="selected"]');await click('[data-action="select-roster"]');await val('[data-field="goal"]','55000');await click('[data-action="propose"]');assert(await page.eval(()=>document.querySelector('[data-action="agree"]').disabled),'consent must start unchecked');await click('[data-action="friend-check"]');await click('[data-check="consent"]');await click('[data-action="agree"]');assert((await get()).joined,'joined');await click('[data-action="demo-sam-agree"]');assert(await page.eval(()=>document.querySelector('#phone-content').textContent.includes('55,000')),'saved target');});
await check('Review-open → submitted request → saved receipt',async()=>{await go('result');await click('[data-action="demo-review"]');await click('[data-go="review"]');assert(await page.eval(()=>document.querySelector('[data-action="save-review"]').disabled),'requires reason');await val('[data-field="reviewReason"]','Some activity is missing');await val('[data-field="reviewNote"]','Please check the Sunday update.');await click('[data-action="save-review"]');assert((await get()).route==='reviewReceipt','receipt');await click('#phone-content [data-go="result"]');assert(await page.eval(()=>document.querySelector('#phone-content').textContent.includes('Return update pending')),'must not fabricate return');});
await check('Safe exit → saved record / return pending',async()=>{await go('challengeDetail');await click('[data-action="exit:friends"]');await click('[data-action="confirm-exit"]');assert((await get()).route==='exitReceipt','exit receipt');assert(await page.eval(()=>document.querySelector('#phone-content').textContent.includes('Update pending')),'return pending');});
await check('Community → rules → consent → confirmation → private progress / suppression',async()=>{await page.eval(()=>SignalDemo.reset());await go('community');await click('#phone-content [data-go="communityRules"]');assert(await page.eval(()=>document.querySelector('[data-action="join-community"]').disabled),'unchecked');await click('[data-action="community-check"]');await click('[data-check="communityConsent"]');await click('[data-action="join-community"]');await click('#phone-content [data-go="communityProgress"]');await val('#community-privacy','suppressed');assert(await page.eval(()=>!document.querySelector('#phone-content').textContent.includes('12')),'no exact count');});
await check('Existing → pending result → review → receipt → payment separate',async()=>{await go('legacyDetail');await click('[data-action="demo-legacy-review"]');await click('#phone-content [data-go="legacyReview"]');await click('[data-action="legacy-review-save"]');assert((await get()).route==='legacyReceipt','legacy receipt');await click('#phone-content [data-go="legacyPayment"]');assert(await page.eval(()=>document.querySelector('#phone-content').textContent.includes('settlement paused')),'legacy paused');});
await check('Historical setup → exact consent → saved replay → cancellation receipt',async()=>{await go('legacySetup');assert(await page.eval(()=>document.querySelector('[data-action="legacy-start"]').disabled),'historical consent off');await click('[data-check="legacyConsent"]');await click('[data-action="legacy-start"]');await click('#phone-content [data-go="legacyExit"]');await click('[data-action="legacy-exit-save"]');assert((await get()).route==='legacyExitReceipt','legacy exit receipt');});
await check('Staged friend creation defaults → saved lobby',async()=>{await page.eval(()=>SignalDemo.reset());await go('createType');assert((await get()).draft.target==='','blank default');await click('[data-action="create-next"]');await click('[data-action="create-next"]');await click('[data-action="create-next"]');await click('[data-action="create-next"]');await click('[data-action="create-save"]');assert((await get()).route==='createSaved','saved');await click('#phone-content [data-go="createdLobby"]');assert((await get()).created.amount==='20','default amount');await click('[data-action="create-link"]');assert(await page.eval(()=>document.querySelector('#phone-content').textContent.includes('hasn’t been sent')),'no delivery claim');});
await check('Staged Personal validation → readiness → explicit consent → immutable receipt',async()=>{await page.eval(()=>SignalDemo.reset());await go('createType');await click('[data-action="create-kind:personal"]');await click('[data-action="create-next"]');await click('[data-action="create-next"]');assert((await get()).route==='createActivity','blank target blocked');await val('[data-field="draft.target"]','12345');await click('[data-action="create-next"]');await click('[data-action="create-next"]');await click('[data-action="create-next"]');assert(await page.eval(()=>document.querySelector('[data-action="create-save"]').disabled),'save needs readiness/consent');await click('[data-action="create-check"]');assert(await page.eval(()=>document.querySelector('[data-action="create-save"]').disabled),'readiness not consent');await click('[data-check="createConsent"]');await click('[data-action="create-save"]');assert((await get()).created.target==='12345','snapshot');await go('createActivity');await val('[data-field="draft.target"]','60000');await go('createSaved');assert(await page.eval(()=>document.querySelector('#phone-content').textContent.includes('12,345')),'receipt immutable');});
console.log(JSON.stringify(results,null,2));
